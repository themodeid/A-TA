meta {
  name: 01-Get All Periode
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/payroll/periode
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
