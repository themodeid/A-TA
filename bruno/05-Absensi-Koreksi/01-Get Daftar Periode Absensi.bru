meta {
  name: 01-Get Daftar Periode Absensi
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/absensi/periode/list
  body: none
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

tests {
  test("Status code is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
