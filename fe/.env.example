# Salin ke fe/.env.local jika ingin override (lihat juga .env.example di root project).
# Nilai default untuk npm run dev ada di .env.development.
PORT=3021
NEXT_PUBLIC_API_URL=http://localhost:3020/api/kontak

# Opsional: di Docker, backend dicapai lewat nama service (lihat docker-compose).
# INTERNAL_API_URL=http://backend:3020/api/kontak
